<% if @project.image.attached? %>
  <%= image_tag @project.image, width: 300 %>
<% end %>

<h1><%= @project.name %></h1>

<p><strong>Description:</strong></p>
<p><%= @project.description.present? ? @project.description : "No description provided." %></p>
# <% if current_user.qa? %>
#   <!-- Add QA-specific content here if needed -->
# <% end %>

<%= link_to "Back to Projects", projects_path %>
